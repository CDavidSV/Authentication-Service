services:
  db:
    image: postgres:latest
    container_name: "auth_db"
    ports:
        - "5432:5432"
    restart: always
    environment:
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: 12345678
        POSTGRES_DB: auth_db